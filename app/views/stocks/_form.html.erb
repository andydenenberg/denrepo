<%= form_with(model: stock, local: true) do |form| %>
  <% if stock.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(stock.errors.count, "error") %> prohibited this stock from being saved:</h2>

      <ul>
      <% stock.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :symbol %>
    <%= form.text_field :symbol %>
  </div>

  <div class="field">
    <%= form.label :last_price %>
    <%= form.text_field :last_price %>
  </div>

  <div class="field">
    <%= form.label :last_change %>
    <%= form.text_field :last_change %>
  </div>

  <div class="field">
    <%= form.label :update %>
    <%= form.datetime_select :last_updated %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
